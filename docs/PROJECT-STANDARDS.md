# FocusLink 项目标准

- 产品正式名称统一为 `FocusLink`；`Foxlink` 仅作为历史兼容标识，新增用户文案不得继续使用。
- 云端 MCP、同步适配器和主应用共享同一合同与发布记录。
- Account Durable Object 是业务权威；D1 MCP 投影不得接受独立业务写入。
- MCP 仅暴露明确批准的专注/任务字段，不返回备注、标签、设备标识、凭据或本地诊断。
- 旧本机 MCP 保留为迁移兼容，不得被描述为电脑关机可用。
