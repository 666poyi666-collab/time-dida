# FocusLink v0.12.11

> 发布日期：2026-07-19
>
> 对应提交：`待源码提交后填写`
>
> 发布类型：正式版
>
> 验证状态：门禁执行中

## 本次更新

### v0.12.10 发布目录元数据修复

- 完整继承 v0.12.10 已通过本机验收的四套艺术字体、全局强调色、日报 Dashboard、五套计时仪表、原生全屏沉浸、秒级时间之带与 `280×84` 小窗。
- v0.12.10 的公开 tag 因 `shared/version.ts` 中 `APP_RELEASE_DIR` 仍指向旧目录而在 GitHub 元数据门禁被阻断；旧 tag 保持不变，本版把版本与目录常量同步为 `0.12.11 / release-v01211`，并从新的干净源码提交重新生成资产。

### 设计更新

- Noto Sans SC、霞鹜文楷、霞鹜新致宋与霞鹜漫黑四套字体全部本地嵌入；五种强调色贯穿导航、按钮、任务、统计、设置与专注，暂停保持红色。
- 统计按结论、四项指标、24 小时时间轴、任务投入排行和最近会话表顺读；多日范围显示每日趋势。
- 翻页机械修复旧数字卡住，沉浸模式使用原生全屏和 520ms 入场，时间之带支持近景/远景、暂停秒级更新与状态粒子。
- 展开小窗为 `280×84`，仍完整显示任务、当前时间、三项累计和全部控制。

## 升级提示

- 旧字体值会安全迁移到兼容字体；数据库、计时记录、任务关联与同步队列无迁移。
- 安装版可覆盖升级；便携版可直接替换可执行文件。

## 已知限制

- 番茄 To-do 当前客户端仍没有 PCRecord 远端删除与独立云端回读接口；FocusLink 只把桥明确返回 success 的记录标记为“上传已确认”。

## 验证

- format / typecheck / lint / 294 项自动化测试 / production audit
- Electron 回归、打包主窗/小窗/全屏沉浸/字体/仪表/统计/时间之带 smoke
- dida 与番茄 To-do 真实临时数据验证
- 安装版与便携版由新资产重新复验

## 下载与校验

| 文件 | SHA256 |
| --- | --- |
| `FocusLink-0.12.11-x64.exe` | `待生成` |
| `FocusLink-0.12.11-x64-portable.exe` | `待生成` |

同时提供 `SHA256SUMS.txt`。
