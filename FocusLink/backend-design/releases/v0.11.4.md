# FocusLink v0.11.4

> 发布日期：2026-07-15
>
> 对应提交：正式构建后回填
>
> 发布类型：正式版
>
> 验证状态：候选门禁中

## 本次更新

### 排版驱动的连续专注台

- 大字号时间、局部流动光面和细活动轨替代旧科幻圆轨；状态、任务、计时、控制与累计数据使用统一基线。
- 计时器和本次片段共享工作面；账本使用连续事件行、轻量状态带和本轮连续性进度。

### 单一分析画布

- 每日趋势使用柱、趋势线、面积和刻度组合图；单日稀疏数据保留合理上界。
- 时间构成与单次排行收进同一分析画布，全部来自当前筛选范围内的真实数据。

## 修复

- v0.11.3 公开 tag 在资产校验阶段因 SHA256 清单缺少 GNU 格式的星号文件标记而被阻断；本版使用 workflow 要求的 `<hash> *<filename>` 精确格式重新发布。

## 升级提示

- 用户数据库、任务关联、计时记录、同步队列和小窗尺寸无需迁移。

## 已知限制

- 正式构建、线上 Release 回读完成前保持候选状态。

## 验证

- 完成正式构建后回填。

## 下载与校验

- 完成正式构建后回填。

